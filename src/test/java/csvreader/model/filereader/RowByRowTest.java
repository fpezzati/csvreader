package csvreader.model.filereader;

class RowByRowTest {

	/**
	 * What does it do?
	 * 
	 * - pick an inputstream,
	 * - read row,
	 * - returns row as composition of csvreaders
	 */
	
	/**
	 * when given inpustream is null, sut returns an empty row.
	 */
}
